const User = require('./user.model');
const Charges = require('./charges.model');
const Bill = require('./bill.model');
const EVCCode = require('./evc.model');
const Recharge = require('./recharge.model');

module.exports = {
  User,
  Charges,
  Bill,
  EVCCode,
  Recharge,
};
